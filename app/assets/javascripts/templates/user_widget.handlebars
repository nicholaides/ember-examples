{{#if user.isLoading}}

	<img class="loader" src="/assets/ajax-loader.gif"/>

{{else}}

	<img class="avatar" {{bindAttr src="user.avatarPath"}}/>
	<span class="name">{{user.fullName}}<span>

	{{view App.PokeView userBinding="user"}}
{{/if}}
